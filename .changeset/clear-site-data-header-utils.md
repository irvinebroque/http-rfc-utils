---
'@irvinebroque/http-rfc-utils': minor
---

Add W3C Clear-Site-Data header utilities with tolerant parsing (`parseClearSiteData`) and strict formatting/validation (`formatClearSiteData`, `validateClearSiteData`), including quoted-string enforcement, unknown-directive ignore behavior, malformed-list empty results, and wildcard expansion to known data types.
