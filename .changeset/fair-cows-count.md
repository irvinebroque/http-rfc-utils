---
'@irvinebroque/http-rfc-utils': patch
---

Improve performance across core RFC utilities and add a benchmark suite (`pnpm bench`) to track hot-path regressions.
Expand RFC-focused test coverage around optimized paths to keep behavior stable while improving throughput.
