---
'@irvinebroque/http-rfc-utils': minor
---

Add W3C Reporting API helpers for `Reporting-Endpoints` and `application/reports+json`, including Structured Field dictionary parse/format support, response URL endpoint resolution with potentially trustworthy origin filtering, report URL stripping, and deterministic report payload serialization/parsing with attempts-counter increment semantics.
