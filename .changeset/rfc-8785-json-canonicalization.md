---
'@irvinebroque/http-rfc-utils': minor
---

Add RFC 8785 JSON Canonicalization Scheme helpers for deterministic canonical JSON text/UTF-8 formatting, semantic validation (including lone-surrogate and non-finite number rejection), and tolerant parsing that accepts only already-canonical JSON input.
