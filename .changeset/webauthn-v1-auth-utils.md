---
'@irvinebroque/http-rfc-utils': minor
---

Add WebAuthn v1 auth utilities for strict base64url parsing/formatting, creation/request options JSON codecs with conservative policy validation, clientDataJSON parse/verify helpers, authenticatorData structural parsing with AT/ED handling, and COSE algorithm allowlist validation so RPs can implement deterministic standards-aligned verification primitives without browser/UI orchestration.
